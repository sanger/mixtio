<tr>
  <td><%= fa_icon "flask" %></td>
  <td><%= batch.lot.consumable_type.name %></td>
  <td><%= batch.lot.name %></td>
  <td><%= batch.expiry_date.to_s %></td>
  <td><%= batch.lot.supplier.name if batch.lot.supplier %></td>
  <td><%= time_ago_in_words(batch.created_at) %> ago</td>
</tr>